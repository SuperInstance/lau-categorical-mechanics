[package]
name = "lau-categorical-mechanics"
version = "0.1.0"
edition = "2021"
description = "Categorical foundations unifying symplectic mechanics and agent behaviors"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-categorical-mechanics"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
nalgebra = { version = "0.33", features = ["serde-serialize"] }
